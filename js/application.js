$(document).ready(function() {

  for (i=0;i<)

  var boardView = new BoardView();
  var board = new Board();
  // board.placeNewTile();
  // board.placeNewTile();
  // boardView.renderBoard(board.grid);

});